% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_numeric.R
\name{convert_numeric}
\alias{convert_numeric}
\title{Convert numeric-like columns to numeric}
\usage{
convert_numeric(data, exclude = "mt_id")
}
\arguments{
\item{data}{A data frame or tibble.}

\item{exclude}{An optional character vector of column names to exclude from
conversion. Defaults to \code{NULL}.}
}
\value{
A data frame with numeric-like columns converted to numeric.
}
\description{
Detects columns whose non-missing, non-empty values can be interpreted as
numeric and converts them to numeric format. Before conversion, decimal
commas are replaced with decimal points.
}
\details{
Columns can be excluded from conversion using the \code{exclude} argument.

A column is treated as numeric-like if all of its non-missing and non-empty
values can be converted to numeric after replacing \code{","} with \code{"."}.
This is useful for datasets where numeric values are stored as character
strings, including those using decimal commas.

df <- convert_numeric(df, exclude = "id")
}
