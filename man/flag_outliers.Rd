% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flag_outliers.R
\name{flag_outliers}
\alias{flag_outliers}
\title{Flag move time outliers}
\usage{
flag_outliers(
  data,
  flag_outliers = "none",
  filter_var = "move_time",
  outlier_threshold = 120000
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{flag_outliers}{A character string specifying the outlier detection
method. Must be one of \code{"none"}, \code{"data"}, or \code{"threshold"}.
Defaults to \code{"none"}.}

\item{filter_var}{A character string naming the numeric column on which
outlier detection should be performed. Defaults to \code{"move_time"}.}

\item{outlier_threshold}{A numeric threshold used when
\code{flag_outliers = "threshold"}. Values above this threshold are flagged
as outliers. Defaults to \code{120000}.}
}
\value{
A data frame with the same rows as the input and an additional
logical column, \code{flag_outlier}, indicating whether each row was flagged
as an outlier.
}
\description{
Flags outlier values in a numeric column using one of two criteria:
a data-based rule or a fixed threshold. The function can also return the
data unchanged except for an added outlier flag when no detection criterion
is selected.
}
\details{
If \code{flag_outliers = "data"}, values are flagged as outliers when they are
greater than the mean plus two standard deviations of \code{filter_var}.

If \code{flag_outliers = "threshold"}, values are flagged as outliers when they
are greater than \code{outlier_threshold}.

If \code{flag_outliers = "none"}, no values are flagged and the output column
\code{flag_outlier} is set to \code{FALSE} for all rows.
}
\examples{
# Flag outliers using the data-based rule
# time_data <- flag_outliers(df, flag_outliers = "data")

# Flag outliers using a fixed threshold
# time_data <- flag_outliers(df, flag_outliers = "threshold",
#                            outlier_threshold = 120000)

}
