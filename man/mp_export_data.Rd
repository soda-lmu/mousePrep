% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mp_export_dataset.R
\name{mp_export_data}
\alias{mp_export_data}
\title{Export mousetrap data in long or wide format}
\usage{
mp_export_data(
  data,
  direction,
  incl_measures_wide = FALSE,
  long_join_data = NULL,
  wide_join_data = NULL,
  id = "mt_id"
)
}
\arguments{
\item{data}{A mousetrap object or compatible object containing trajectory
data for export.}

\item{direction}{A character string specifying the export format.
Must be either \code{"long"} or \code{"wide"}.}

\item{incl_measures_wide}{A logical value indicating whether a measures
object should be included in wide exports. Defaults to \code{FALSE}.}

\item{long_join_data}{An optional data frame to join to the long-format
export. Only used when \code{direction = "long"}. Defaults to \code{NULL}.}

\item{wide_join_data}{An optional data frame to join to the wide-format
export. Only used when \code{direction = "wide"}. Defaults to \code{NULL}.}

\item{id}{A single character string naming the join key column present in
both exported data and the join data. Defaults to \code{"mt_id"}.}
}
\value{
A data frame containing the exported mousetrap data in the requested
format, optionally joined with additional data.
}
\description{
Exports a mousetrap object to long or wide format using
\code{mt_export_long()} or \code{mt_export_wide()}. Optionally joins the exported
data with an additional data frame. For wide exports, summary measures can
also be included when available.
}
\details{
When \code{incl_measures_wide = TRUE} for wide exports, the function adds a \code{"measures"} object.
If multiple such objects are found, the first one is used and a warning is issued.
Joins are performed with \code{dplyr::inner_join()}, so only rows with matching
keys in both inputs are retained.
}
\examples{

# Long export
long_data <- mp_export_data(data = mt_obj, direction = "long")

# Wide export with measures
wide_data <- mp_export_data(
  data = mt_data,
  direction = "wide",
  incl_measures_wide = TRUE
)

# Long export with join
long_joined <- mp_export_data(
  data = mt_data,
  direction = "long",
  long_join_data = participant_info,
  id = "mt_id"
)

}
