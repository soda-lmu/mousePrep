% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rm_cases_string.R
\name{rm_cases_string}
\alias{rm_cases_string}
\title{Remove cases from a mouse-movement dataset}
\usage{
rm_cases_string(
  data,
  column_rm,
  factor_rm,
  criteria = c("session", "participant"),
  part_id = "mouseid",
  sess_id = "workerId"
)
}
\arguments{
\item{data}{A data frame containing mouse movement data.}

\item{column_rm}{Column used for the exclusion criterion (unquoted or string).}

\item{factor_rm}{Value(s) in \code{column_rm} that trigger exclusion (vector allowed).}

\item{criteria}{One of:
\itemize{
\item \code{"session"}: remove only the affected sessions (worker IDs)
\item \code{"participant"}: remove all rows for affected participants
}}

\item{part_id}{Participant ID column (default \code{"mouseid"}; unquoted or string).}

\item{sess_id}{Session/worker ID column (default \code{"workerId"}; unquoted or string).}
}
\value{
A tibble/data.frame with excluded rows removed.
}
\description{
Removes rows associated with sessions (worker IDs) or participants when a
filter condition is met (e.g., touch device used, window resized, non-English).
}
\details{
This function is "tidyverse-friendly":
\itemize{
\item You can pass column names \strong{unquoted} (e.g., \code{workerId}) or as \strong{strings}
(e.g., \code{"workerId"}).
\item Uses \code{.data[[...]]} to avoid NSE notes in package checks.
}
}
\examples{
# Remove only sessions where touch-capable devices were used
# rm_cases_string(uas_data, column_rm = userAgent_is_touch_capable, factor_rm = TRUE,
#          criteria = "session")

# Remove entire participants if any of their sessions used a touch device
# rm_cases_string(uas_data, column_rm = "userAgent_is_touch_capable", factor_rm = TRUE,
#          criteria = "participant")

}
